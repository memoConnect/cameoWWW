# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /                      controllers.Application.redirect(url = "/w/")
GET        /w                     controllers.Application.redirect(url = "/w/")
GET        /w/                    controllers.Application.index

GET     /                           controllers.Application.index
GET     /moep                       controllers.Moep.moep
GET     /sendMail                   controllers.Message.sendMessage
POST    /sendMail                   controllers.Message.sendMessage

# Map static resources from the /public folder to the /assets URL path
GET     /w/static/*file           controllers.Assets.at(path="/public/", file)